# Navigator Hub

Navigator Hub 是一个面向开发者与技术爱好者的轻量级导航与资源聚合工具。项目定位为开源技术社区的外延组件，用于统一管理、展示和快速检索互联网上的优质技术文档、社区讨论、工具链入口与公共服务站点。项目目标用户包括运维工程师、全栈开发者、技术决策者以及开源软件贡献者。Navigator Hub 通过结构化的数据组织方式，将分散在多个域名下的有价值信息整合到单一入口，降低信息检索成本，提升技术调研与日常开发的工作效率。

项目核心设计理念为简洁、可扩展与社区驱动。所有资源条目以声明式配置文件定义，支持通过拉取请求的方式由社区共同维护。Navigator Hub 本身不存储任何第三方内容，仅作为索引层和展示层，确保项目轻量化且合规。项目输出的静态页面可部署于任意 Web 服务器或 CDN，亦支持 Docker 容器化运行，适配云原生部署场景。

## 功能概览

- **多维度资源分类**：按技术领域、使用频率、受众群体等维度对资源条目进行标签化管理，支持快速筛选与定位。
- **关键词全文检索**：内置基于内存索引的轻量级搜索功能，支持对资源标题、描述、标签和域名进行模糊匹配查询。
- **自定义分类视图**：允许用户根据自身关注领域创建专属分类视图，仅展示特定标签或域下的资源集合。
- **资源可用性检测**：定期对已收录资源进行 HTTP 头部探测，标记异常或响应超时的条目，辅助管理员清理失效链接。
- **导入与导出机制**：支持以 JSON 或 YAML 格式批量导入资源列表，亦支持将当前分类数据导出为结构化文件，便于迁移或备份。
- **访问统计聚合**：基于前端页面访问日志，聚合展示高频点击资源 Top 排名，帮助社区发现热门工具与文档。
- **响应式布局适配**：页面布局针对桌面端、平板与移动设备进行自适应优化，确保在不同屏幕尺寸下的浏览体验一致。
- **暗色主题切换**：内置亮色与暗色两套视觉主题，跟随系统偏好或由用户手动切换，降低长时间使用时的视觉疲劳。

## 应用场景

- **技术团队内部知识库入口**：团队可将日常开发中常用的API文档、CI/CD工具链地址、监控面板链接等统一收录至Navigator Hub，作为团队内部浏览器起始页，减少寻找内部系统地址的时间。
- **开源项目文档聚合页**：开源项目维护者可利用Navigator Hub整理项目相关的设计文档、RFC提案、社区会议记录、外部依赖说明等资源，为贡献者提供一站式导航，降低新成员上手门槛。
- **技术培训与教学辅助**：培训机构或高校教师可将课程所需的外部参考网站、在线编译器、代码示例仓库等资源通过Navigator Hub组织成课程专用视图，学员无需自行搜集地址，可直接聚焦学习内容。
- **个人开发环境起始页**：独立开发者可自定义配置，将常用的云服务控制台、代码托管平台、容器镜像仓库、日志分析工具等入口集中管理，配合暗色主题与搜索功能，提升个人工作流效率。

## 快速开始

以下指令适用于 Linux / macOS / Windows WSL 环境。请确保系统已安装 Git 与 Node.js（版本 >= 18.x）。

```bash
# 1. 克隆项目仓库
git clone https://github.com/navigator-hub/navigator-hub.git

# 2. 进入项目目录
cd navigator-hub

# 3. 安装项目依赖
npm install

# 4. 启动开发服务器
npm run dev
```

执行上述步骤后，开发服务器将默认监听 `http://localhost:3000`。打开浏览器访问该地址即可预览导航页面。若要构建生产环境静态文件，请执行 `npm run build`，产物将输出至 `dist` 目录，可直接部署至任意静态托管服务。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | >= 18.0.0 | 项目运行时与构建工具链基于 Node.js 生态，需确保版本满足要求 |
| npm | >= 9.0.0 | 包管理器，用于安装项目依赖与执行脚本命令 |
| Git | >= 2.30.0 | 版本控制工具，用于克隆仓库及后续拉取最新更新 |
| 浏览器 | 现代浏览器（Chrome / Firefox / Edge / Safari 最新两个主要版本） | 运行前端页面，需支持 ES6 模块与 CSS Grid / Flexbox 布局 |
| 操作系统 | Linux / macOS / Windows（WSL2 或原生 Windows 11） | 开发环境推荐 Unix-like 系统，生产环境无特殊限制 |
| 网络 | 可访问公网 | 项目本身不依赖外网，但部分资源链接需网络可达 |
| 内存 | >= 512 MB | 开发服务器运行时的最低内存要求，生产构建时建议 >= 1 GB |
| 磁盘空间 | >= 200 MB | 包含源代码、依赖包及构建产物的总占用量 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 入门指南 | `docs/getting-started.md` | 如何快速部署一个可用的导航实例？初始配置文件如何编写？ |
| 配置参考 | `docs/configuration.md` | 支持哪些配置字段？分类、标签、资源条目的数据结构定义是什么？ |
| 自定义主题 | `docs/theming.md` | 如何修改页面颜色、字体与布局？如何新增一套完全自定义的视觉主题？ |
| API 接口 | `docs/api.md` | 开发模式下提供了哪些内部接口？如何扩展搜索或检测逻辑？ |
| 部署指南 | `docs/deployment.md` | 支持哪些部署方式（Nginx、Docker、Vercel、Cloudflare Pages）？如何配置环境变量？ |
| 常见工作流 | `docs/workflows.md` | 如何批量添加资源？如何定期检测失效链接？如何备份用户自定义配置？ |
| 贡献规范 | `CONTRIBUTING.md` | 提交拉取请求的流程是什么？代码风格与提交信息格式有何要求？ |

## 资源列表

以下为项目收录的全部外部资源链接，按类别分组展示。所有链接均按照用户原始数据原样列出，未做任何协议、域名或路径修改。

影视资源类

- <code>yinghuadongmanguanfangbana.org.cn</code>
- <code>zhongwenzimuyongjiuzaixiana.org.cn</code>
- <code>mianfeizhuijuwangzhana.org.cn</code>
- <code>gaoqingzhongwenzimua.org.cn</code>
- <code>zaixianbofangnidongdea.org.cn</code>
- <code>zhongwenzimuzaixianmianfeikanb.org.cn</code>
- <code>zaixianshipinzhongwenzimub.org.cn</code>

## 项目结构

项目采用模块化目录组织，核心代码与配置分离，便于维护与扩展。以下为项目主要目录与文件说明：

```
navigator-hub/
├── public/                         # 静态资源目录，不经过构建处理
│   ├── favicon.ico                 # 站点图标
│   └── robots.txt                  # 搜索引擎爬虫规则
│
├── src/                            # 源代码主目录
│   ├── assets/                     # 样式、图片、字体等静态资产
│   │   ├── styles/                 # 全局样式与主题变量（CSS / SCSS）
│   │   └── images/                 # Logo、背景图等图像资源
│   │
│   ├── components/                 # 可复用 UI 组件
│   │   ├── Layout/                 # 整体布局组件（头部、侧栏、底部）
│   │   ├── ResourceList/           # 资源列表渲染与分页组件
│   │   ├── SearchBar/              # 搜索输入与结果下拉组件
│   │   ├── CategoryFilter/         # 分类筛选与标签过滤组件
│   │   └── ThemeToggle/            # 亮色/暗色主题切换按钮
│   │
│   ├── data/                       # 资源数据与配置管理
│   │   ├── resources.json          # 核心资源条目数据（支持导入导出）
│   │   ├── categories.json         # 分类与标签定义
│   │   └── schema/                 # 数据结构的 JSON Schema 验证文件
│   │
│   ├── hooks/                      # 自定义 React / Vue Hooks（根据框架调整）
│   │   ├── useSearch.ts            # 搜索逻辑封装
│   │   ├── useFilter.ts            # 分类筛选逻辑封装
│   │   └── useHealthCheck.ts       # 资源可用性检测逻辑
│   │
│   ├── pages/                      # 页面路由入口
│   │   ├── index.tsx               # 主首页，展示所有资源
│   │   ├── category/[id].tsx       # 按分类 ID 筛选的视图
│   │   └── about.tsx               # 项目介绍与关于页面
│   │
│   ├── services/                   # 业务逻辑与外部接口服务
│   │   ├── resourceService.ts      # 资源增删改查与持久化服务
│   │   └── detectService.ts        # 链接可用性探测服务
│   │
│   ├── utils/                      # 工具函数集合
│   │   ├── formatter.ts            # 日期、字符串格式化工具
│   │   ├── validator.ts            # URL、标签等数据校验工具
│   │   └── storage.ts              # localStorage 读写封装
│   │
│   └── main.tsx                    # 应用入口文件，挂载根组件
│
├── tests/                          # 单元测试与集成测试
│   ├── unit/                       # 组件与工具函数的单元测试
│   └── integration/                # 页面流程与 API 集成测试
│
├── docs/                           # 项目文档（对应文档导航章节）
├── scripts/                        # 辅助脚本（数据迁移、构建优化等）
├── .env.example                    # 环境变量示例文件
├── package.json                    # 项目依赖与脚本定义
├── tsconfig.json                   # TypeScript 编译配置
├── vite.config.ts                  # 构建工具（Vite）配置文件
└── README.md                       # 本项目自述文件
```

## 贡献指南

项目遵循开源社区协作规范，欢迎任何形式的贡献，包括但不限于新增资源条目、修复缺陷、完善文档与提出改进建议。具体贡献流程如下：

1. **创建议题讨论**：在提交拉取请求之前，请先在 Issues 区域创建对应议题，描述您计划修改的内容或新增的功能，与维护者及其他贡献者达成共识，避免重复工作或不必要的代码冲突。
2. **派生仓库并创建分支**：将主仓库派生至个人账户，然后基于 `main` 分支创建您的工作分支。分支命名建议采用 `<type>/<short-description>` 格式，例如 `feat/add-ai-resources` 或 `fix/search-case-sensitivity`。
3. **编写代码并遵循规范**：所有代码必须通过 ESLint 与 Prettier 检查，并确保新增或修改的功能有对应的单元测试覆盖。提交信息需遵循 Conventional Commits 规范，使用 `feat:`、`fix:`、`docs:`、`chore:` 等前缀。
4. **提交拉取请求**：将您的工作分支推送至派生仓库，然后向主仓库的 `main` 分支提交拉取请求。请在请求描述中清晰引用关联议题编号，并详细说明改动内容与测试结果。
5. **代码审查与合并**：维护者将在一周内进行审查，可能提出修改意见。通过审查后，拉取请求将由维护者合并至主分支，您的贡献将被记录在贡献者列表中。

## 常见问题

**问：Navigator Hub 是否存储或缓存第三方网站的内容？**

答：项目不存储任何第三方网站的内容、截图或缓存数据。Navigator Hub 仅记录用户提供的 URL 地址与元数据（标题、描述、标签），所有访问均由用户浏览器直接发起。可用性检测功能仅返回 HTTP 状态码，不下载页面正文内容。

**问：如何批量更新资源列表？是否可以与现有书签系统同步？**

答：项目支持通过 JSON 或 YAML 文件批量导入资源。您可以从浏览器导出书签为 HTML 文件，然后使用社区提供的转换脚本（位于 `scripts/convert-bookmarks.js`）将书签转换为 Navigator Hub 的数据格式。目前暂不支持与浏览器书签系统实时自动同步，但可通过定时导入的方式保持近似一致。

**问：部署后搜索功能无法正常返回结果，可能的原因是什么？**

答：首先检查浏览器控制台是否有 JavaScript 报错，确保 `resources.json` 文件已正确加载且格式合法。其次，搜索功能默认区分大小写，请确认关键词大小写与资源标题一致。若资源条目数量超过 5000 条，建议启用服务端搜索模式（需配置后端 API），否则浏览器端内存索引可能影响性能。详细配置请参考 `docs/deployment.md` 中的搜索调优章节。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-24 22:42:18
