# NexusIndex

NexusIndex 是一个面向技术内容创作者、本地化团队与数字档案管理者的高密度外链资源聚合与导航系统。该项目并非传统意义上的爬虫或采集器，而是一套专注于“人工精选外链资源”的索引编排框架。其核心目标是为特定垂直领域（如多媒体字幕资源、跨语言视听素材、开源本地化工具链）提供可维护、可审计、可快速恢复的参考链接清单，帮助用户从冗余、过时或碎片化的浏览器书签中解放出来，构建一套具备版本控制能力的外部资源引用基线。

NexusIndex 默认以静态站点骨架或纯 Markdown 索引库形态运行，适合作为团队内部的知识底座，也可公开部署为轻量级导航页。项目不依赖重型数据库，所有链接条目以结构化 Markdown 表格与 YAML Frontmatter 进行描述，便于通过 Git 进行变更追溯与 PR 评审。对于频繁处理多语言字幕文件、跨地区流媒体源整理、或需要定期校验外部链接可用性的运维人员，NexusIndex 提供了一套严格的 URL 清单管理规范与自动化链接巡检脚本模板。

## 功能概览

- **分层索引目录**：支持按语种、地区、文件格式、来源站点性质等维度建立多级分类，每条链接可归属多个虚拟标签组，便于不同角色（如译员、审核员、归档员）快速定位。
- **外链状态快照**：为每个收录的 URL 提供可选的备注字段，用于记录最后一次访问时间、响应码、页面标题摘要，辅助判断资源存活状态。
- **批量导入与去重**：提供 CSV / TSV 导入接口，自动识别并合并重复条目，冲突时保留用户指定的高优先级字段值。
- **链接关系图谱**：自动生成内部引用关系报告，展示不同资源之间的互补或替代关联，辅助决策主备用源。
- **变更审计日志**：所有增删改操作均记录操作人、时间戳与变更原因，满足团队协作下的合规追溯需求。
- **自定义元数据模板**：允许用户为不同分类定义不同的必填属性集（如“字幕语言”“帧率”“授权类型”），使索引结构贴合实际业务语义。
- **离线快照导出**：支持将当前索引全量导出为静态 HTML 或纯文本清单，便于脱机环境下的分发与备份。

## 应用场景

- **多语言字幕资源整理**：影视翻译团队需要持续跟踪多个字幕源站点的更新情况。NexusIndex 可为每个站点建立独立的条目卡片，记录其语言覆盖范围、更新频率与历史可靠性，大幅减少重复性查找时间。
- **跨地区流媒体源备选**：内容分发运营人员需维护一组备用播放源，以应对主线路限流或区域封锁。通过 NexusIndex 的标签系统与备注字段，可清晰标记每个源的可用区域、清晰度档次与延迟等级。
- **开源本地化工具链文档索引**：技术文档团队在翻译开源项目时，常需引用大量术语库、风格指南、翻译记忆库。NexusIndex 可作为统一入口，集中管理这些外部依赖的原始发布地址与镜像地址。
- **学术研究资料归档**：研究者收集特定主题下的多媒体语料资源时，使用 NexusIndex 记录每个资源的出处、抓取日期与版权声明，避免因链接失效导致的研究数据不可复现问题。

## 快速开始

以下步骤适用于在 Linux / macOS / Windows WSL2 环境下部署 NexusIndex 基础索引库。

```bash
# 克隆项目仓库
git clone https://github.com/nexusindex/core.git nexusindex-core
cd nexusindex-core

# 安装依赖（Python 3.9+ 环境）
pip install -r requirements.txt

# 初始化示例索引数据（包含预置分类与占位条目）
python scripts/init_db.py --sample

# 启动本地预览服务（默认监听 127.0.0.1:8080）
python app.py serve --port 8080
```

启动后，访问 <code>http://127.0.0.1:8080</code> 可查看索引面板。生产环境部署建议使用 Gunicorn 或 uWSGI 配合 Nginx 反向代理，具体参考 `deploy/` 目录下的示例配置。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | >= 3.9, < 3.13 | 核心运行环境，用于索引解析、校验与 Web 服务 |
| pip | >= 22.0 | Python 包管理工具，用于安装依赖库 |
| Git | >= 2.30 | 版本控制，用于克隆仓库及提交索引变更 |
| Markdown 解析库 | >= 3.4 | 用于渲染索引条目中的富文本备注字段 |
| YAML 处理库 | >= 6.0 | 用于读取分类定义与条目元数据配置 |
| 可选：Redis | >= 6.2 | 若启用链接状态缓存或分布式锁，需额外配置 |
| 可选：SQLite | 3.35+ | 若启用本地持久化审计日志，默认使用 SQLite 存储 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | `docs/user/` | 如何新增资源链接、批量导入、生成快照报表 |
| 运维指南 | `docs/ops/` | 如何部署到生产、配置 HTTPS、设置自动化链接巡检 |
| 开发者参考 | `docs/dev/` | 索引条目 Schema 定义、插件扩展接口、API 路由说明 |
| 设计决策 | `docs/design/` | 为何采用多标签而非树形分类、链接去重策略取舍 |
| 常见工作流 | `docs/workflows/` | 月度外链复核流程、新成员加入时的索引同步步骤 |

## 资源列表

### 多媒体字幕资源（中文字幕）

- <code>zaixianshipinzhongwenzimub.org.cn</code>
- <code>zaixianbofangzhongwenzimub.org.cn</code>
- <code>zhongwenshipinzaixianguankanb.org.cn</code>
- <code>shipinmianfeizaixianguankanb.org.cn</code>

### 日韩影视资源

- <code>rimanzaixianguankanb.org.cn</code>
- <code>rihanzaixianmianfeishipinb.org.cn</code>

### 字幕播放工具资源

- <code>zhongwenzimumianfeibofangb.org.cn</code>

## 项目结构

```
nexusindex-core/
├── app/                                # 主应用模块
│   ├── routes/                         # 路由层：处理 HTTP 请求分发
│   ├── models/                         # 数据模型：定义条目、标签、日志结构
│   ├── services/                       # 业务逻辑：去重、校验、导入导出
│   └── utils/                          # 工具函数：URL 规范化、时间处理
├── config/                             # 配置文件目录
│   ├── default.yaml                    # 默认全局配置（端口、缓存策略）
│   └── schema/                         # 条目元数据 JSON Schema 定义
├── data/                               # 索引数据存储（默认 Git 跟踪）
│   ├── entries/                        # 按分类存放的 Markdown 条目文件
│   ├── tags/                           # 标签定义与层级关系
│   └── audit.log                       # 结构化变更日志（CSV 格式）
├── scripts/                            # 运维与开发辅助脚本
│   ├── check_links.py                  # 批量外链存活检测
│   ├── import_from_csv.py              # CSV 导入工具
│   └── generate_static.py              # 静态站点生成器
├── tests/                              # 单元测试与集成测试
│   ├── test_models/                    # 数据模型层测试
│   └── test_services/                  # 业务逻辑层测试
├── docs/                               # 完整文档（见上文文档导航）
├── deploy/                             # 生产部署示例（Dockerfile / compose）
├── requirements.txt                    # Python 依赖清单
└── README.md                           # 项目说明（本文件）
```

## 贡献指南

1. **分支规范**：从 `main` 分支创建 `feature/` 或 `fix/` 前缀的新分支，命名需简要描述改动目标（例如 `feature/add-http-proxy-check`）。
2. **索引条目变更**：若新增或修改外链条目，需同步更新对应分类下的 Markdown 文件，并在提交信息中注明引用来源或变更理由。
3. **代码风格**：Python 代码遵循 PEP 8 规范，提交前请运行 `scripts/lint.sh` 进行静态检查，确保无严重警告。
4. **测试覆盖**：新增功能或修复缺陷时，需补充至少一个单元测试用例，保证核心解析与去重逻辑的覆盖率不低于 80%。
5. **文档同步**：若改动涉及用户可见行为（如命令参数变化、配置项新增），需同步更新 `docs/user/` 下对应章节，并确保示例代码可执行。

## 常见问题

**Q：NexusIndex 是否提供在线演示环境？**  
A：目前官方未部署公开演示实例，但您可按照快速开始步骤在本地完整运行。对于需要多人协作文档的场景，建议将 `data/` 目录单独作为 Git 子模块，与主应用逻辑解耦。

**Q：如何处理收录的外部链接发生永久迁移（301/308）？**  
A：项目脚本 `scripts/check_links.py` 支持跟踪重定向，并将最终落地 URL 记录至备注字段。您可手动审核后决定是否更新原始条目，系统不会自动替换，以避免误改人工确认的引用地址。

**Q：能否将 NexusIndex 与现有的 Wiki 或 CMS 系统集成？**  
A：可以。NexusIndex 提供 RESTful 风格的导出接口（JSON / RSS），您可通过配置定时任务拉取索引数据，再通过 API 推送至 Confluence、Notion 或自建后台。具体集成示例请参考 `docs/dev/integration.md`。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-24 22:42:35
