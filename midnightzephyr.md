# ReSource Gateway

ReSource Gateway 是一个面向中文互联网技术社区与内容创作者的轻量级外链资源聚合与导航工具。项目定位为“技术化书签托管中间件”，主要服务于个人开发者、小型技术团队以及内容运营人员，用于将分散在多个平台上的外部视频、文档、数据源等资源，以可维护、可版本化、可审查的方式统一收录与管理。项目本身不存储任何多媒体文件或用户生成内容，仅提供结构化的外部链接索引与元数据描述，适用于构建私有或半公开的资源导航站、技术周报外链库、项目依赖的参考文档集合等场景。

## 功能概览

- **结构化外链管理**：支持按类别、标签、来源平台对链接进行多维度分类，并提供 Markdown 与 YAML 两种导出格式。
- **链接状态健康检查**：内置异步 HTTP 头探测任务，可定期检查收录链接的可达性、重定向状态与响应时间，并生成状态报告。
- **元数据自动补全**：对支持 Open Graph 和 JSON-LD 的页面，自动抓取标题、描述与预览图信息，减少手动录入成本。
- **私有化导航页面生成**：基于 Handlebars 模板引擎，将链接数据渲染为静态 HTML 导航页，可直接部署到 Nginx 或对象存储服务。
- **链接变更追踪**：记录每次链接新增、删除或元数据修改的操作日志，支持通过 Git 提交历史进行回溯审计。
- **权限分级查看**：支持公开链接与内部链接两种可见性级别，内部链接可配置 IP 白名单或简单的 Token 鉴权。
- **批量导入与导出**：支持从 CSV 和 JSON 文件批量导入链接清单，也可将当前全部索引导出为通用交换格式。

## 应用场景

- **技术团队内部文档库**：开发团队可将常用的 API 参考文档、设计规范、运维手册等外部链接统一收录，新成员入职时通过导航页快速访问所有必要资源，减少沟通成本。
- **开源项目外部依赖索引**：开源项目维护者可将项目所引用的数据集、预训练模型、第三方服务面板等链接集中管理，并在项目文档中嵌入该导航页，方便贡献者与用户查找。
- **技术周报/月刊资源整理**：内容创作者在编写技术资讯周报时，可将本周提及的所有参考文章、视频、工具站点一次性录入系统，自动生成带有时间戳的归档链接列表，便于读者查阅原文。
- **个人知识库外链备份**：知识管理爱好者可将散落在浏览器书签、笔记软件中的数千条链接迁移至本系统，利用健康检查功能定期发现失效链接，保持知识库的清洁与可用性。
- **线下活动资源分发**：技术沙龙或黑客松组织者可将活动所需的演讲资料、代码仓库、在线协作板等链接预置到导航页中，参会者通过一个入口即可获取全部材料，无需逐个询问。

## 快速开始

以下步骤将在本地环境中启动 ReSource Gateway 的开发实例，默认监听 8080 端口。

```bash
# 克隆代码仓库
git clone https://github.com/resource-gateway/resource-gateway.git
cd resource-gateway

# 安装项目依赖（使用 npm）
npm install

# 复制环境变量模板并填写必要的配置项
cp .env.example .env

# 初始化本地 SQLite 数据库并导入示例链接数据
npm run db:init
npm run db:seed

# 启动开发服务器
npm run dev
```

启动后，访问 `http://localhost:8080` 即可看到示例导航页面。如需修改端口，请在 `.env` 文件中设置 `PORT` 变量。

## 安装要求

| 依赖 | 必需版本 | 说明 |
|------|----------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，需支持 ES2022 特性 |
| npm | 9.x 或更高 | 包管理器，用于安装依赖和执行脚本 |
| SQLite 3 | 系统自带或通过 better-sqlite3 捆绑 | 默认嵌入式数据库，无需额外安装服务端进程 |
| Git | 2.30 或更高 | 用于版本控制和贡献流程中的分支管理 |
| 网络连通性 | 出站 443/80 端口可达 | 用于链接健康检查与元数据抓取，内网环境需配置代理 |
| 内存 | 最低 512 MB，推荐 1 GB | 生产环境下元数据抓取并发数较高时建议提升内存 |
| 存储 | 最低 100 MB 可用空间 | 主要用于存放 SQLite 数据库和静态模板文件，链接数量超过 5 万条时建议扩展 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `/docs/user-guide/` | 如何添加链接、分类管理、生成导航页、配置可见性权限 |
| 运维指南 | `/docs/ops-guide/` | 如何配置健康检查频率、调整并发抓取数、备份与恢复数据库 |
| 开发文档 | `/docs/dev-guide/` | 项目架构说明、核心模块接口、新增数据源解析器的步骤 |
| API 参考 | `/docs/api-reference/` | RESTful API 的端点列表、请求参数格式、错误码与响应示例 |
| 部署示例 | `/docs/deployment/` | 使用 Docker 或 Systemd 部署到 Linux 服务器的完整配置文件模板 |

## 资源列表

本索引库当前收录以下外部资源链接，按内容类别分组展示。所有链接均来自用户提供的原始数据，未经任何改写。

### 在线视频与直播类
- <code>rimanzaixianguankand.org.cn</code>
- <code>rihanzaixianmianfeishipind.org.cn</code>
- <code>zhongwenzimumianfeibofangd.org.cn</code>

### 字幕与衍生内容类
- <code>renqixiliezhongwenzimud.org.cn</code>
- <code>wuyefulizhibod.org.cn</code>

### 影视剧与动画类
- <code>lalalazhongwendianshijud.org.cn</code>
- <code>yinghuadongmanguanfangband.org.cn</code>

## 项目结构

```
resource-gateway/
├── src/                                  # 核心源代码目录
│   ├── core/                             # 核心调度与生命周期管理模块
│   │   ├── app.js                        # 应用主入口，初始化 Express 与中间件
│   │   └── scheduler.js                  # 定时任务调度器（健康检查、元数据更新）
│   ├── routes/                           # RESTful 路由定义层
│   │   ├── links.js                      # 链接的增删改查路由
│   │   ├── categories.js                 # 分类管理路由
│   │   └── health.js                     # 健康检查状态查询路由
│   ├── services/                         # 业务逻辑服务层
│   │   ├── linkService.js                # 链接索引、查询与版本控制逻辑
│   │   ├── fetchService.js               # 外部页面抓取与元数据解析服务
│   │   └── renderService.js              # 静态导航页渲染服务
│   ├── adapters/                         # 外部依赖适配器层
│   │   ├── dbAdapter.js                  # SQLite 数据库操作封装
│   │   ├── httpAdapter.js                # 基于 axios 的 HTTP 客户端封装
│   │   └── cacheAdapter.js               # 内存缓存与可选的 Redis 适配器
│   ├── models/                           # 数据模型定义（验证器与转换器）
│   │   ├── linkModel.js                  # 链接实体校验规则与序列化
│   │   └── categoryModel.js              # 分类实体层级校验
│   └── utils/                            # 通用工具函数
│       ├── logger.js                     # 结构化日志工具（基于 pino）
│       ├── validator.js                  # 自定义输入校验函数
│       └── configLoader.js               # 多环境配置文件加载器
├── templates/                            # Handlebars 导航页模板目录
│   ├── layouts/                          # 全局布局模板
│   └── partials/                         # 可复用的卡片与分页组件
├── docs/                                 # 完整文档目录（覆盖用户、运维、开发三层面）
├── scripts/                              # 辅助运维脚本
│   ├── db-init.js                        # 首次建表与索引脚本
│   ├── db-seed.js                        # 示例数据填充脚本
│   └── health-check-runner.js            # 手动触发全量健康检查的命令行工具
├── test/                                 # 单元测试与集成测试用例
├── .env.example                          # 环境变量配置模板
├── package.json                          # 项目清单与依赖声明
├── README.md                             # 本文件
└── LICENSE                               # MIT 许可证文本
```

## 贡献指南

我们欢迎任何形式的贡献，包括但不限于新增功能、修复缺陷、改进文档与补充测试用例。请遵循以下流程提交贡献：

1. **查阅现有 Issue 与 Project Board**：访问 GitHub 上的 Issue 列表和项目看板，确认您要解决的问题或希望新增的功能尚未被他人认领。如有必要，请新建一个 Issue 并描述您的改进建议，等待维护者反馈后再开始编码，以避免重复劳动。

2. **派生项目并创建功能分支**：将本项目 Fork 到您自己的 GitHub 账户下，然后基于 `main` 分支创建一个新的分支，分支名称应遵循 `feature/功能简述` 或 `fix/问题简述` 的格式。例如 `feature/support-json-export`。

3. **编写代码与单元测试**：所有新增逻辑必须包含对应的单元测试，测试文件存放于 `test/` 目录下，使用 Mocha 或 Jest 框架。确保所有现有测试仍能通过，并且新增代码的测试覆盖率不低于 80%。

4. **更新文档与示例**：如果您的修改涉及用户可见的行为变化（如新增配置项或 API 端点），请同步更新 `docs/` 目录下的相关文档，并在 `.env.example` 中添加新变量的注释说明。对于影响快速开始流程的修改，请同时更新本 README 的对应章节。

5. **发起 Pull Request**：将您的分支推送到派生仓库，然后向本项目的 `main` 分支发起 Pull Request。PR 标题应简明扼要地概括改动内容，正文中请引用相关的 Issue 编号，并附上本地测试通过的结果截图或日志片段。维护者将在 3 个工作日内进行审查。

## 常见问题

**问：健康检查功能会对外部站点造成大量请求压力吗？我应该如何调整检查频率？**

答：健康检查默认采用指数退避重试策略，且只发送 HEAD 请求（不下载响应体），对目标站点的资源消耗极小。默认情况下，系统每 24 小时对所有链接执行一轮全量检查，且并发数限制为 5 个请求。您可以通过环境变量 `HEALTH_CHECK_INTERVAL_MS`（单位毫秒）调整检查间隔，通过 `HEALTH_CHECK_CONCURRENCY` 调整并发数。建议将间隔设置为不低于 6 小时，避免被目标站点误判为恶意爬虫。

**问：我添加的链接被标记为“不可达”，但用浏览器可以正常打开，是什么原因？**

答：出现这种情况通常有以下几种原因。第一，目标站点启用了 Cloudflare 或类似的反机器人防护机制，会拦截非浏览器 User-Agent 的请求。您可以在环境变量中配置 `FETCH_USER_AGENT` 为常见浏览器的 UA 字符串来尝试绕过。第二，目标站点对境外 IP 或特定地理位置的访问有限制，导致健康检查服务器无法直连。这种情况下，您可以考虑使用代理配置（`FETCH_PROXY_URL`）或手动将该链接的状态设置为“忽略检查”。第三，目标站点的响应时间过长，超过了默认的 10 秒超时阈值，您可以通过 `FETCH_TIMEOUT_MS` 适当增大超时时间。

**问：导航页的静态 HTML 如何实现自定义样式与品牌标识？**

答：所有导航页的渲染基于 `templates/` 目录下的 Handlebars 模板。您可以直接修改 `layouts/default.hbs` 中的 CSS 样式以及 HTML 结构。如果希望完全脱离默认样式，可以替换整个 `partials/` 文件夹中的组件。项目本身不强制绑定任何 UI 框架，您可以使用纯 CSS、Tailwind 或 Bootstrap 等任意工具进行定制。修改模板后，只需重新运行 `npm run render` 命令即可重新生成所有静态页面，无需重启服务进程。

> 外链数量: 7 | 生成时间: 2026-08-24 22:42:26
