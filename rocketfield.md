# NexusIndex

NexusIndex 是一个面向开发人员与技术研究者的轻量级外链资源聚合与导航系统。项目定位为“技术资源索引即服务”，旨在通过结构化数据与可解析的链接清单，帮助用户快速定位特定领域的优质外部资源，避免重复检索与信息过载。目标用户包括开源项目维护者、技术文档编写者、数据采集工程师以及需要稳定外链参考的科研人员。NexusIndex 不存储任何第三方内容，仅提供元数据与链接映射关系，所有资源指向均由用户提交的原始 URL 决定。

## 功能概览

- **批量链接导入与去重**：支持通过纯文本或 CSV 批量提交 URL，系统自动识别协议头、域名与路径，过滤重复项并生成唯一标识。
- **分类标签与多级目录**：每个链接可绑定一个主分类与多个自定义标签，支持按技术领域、资源类型、语言或维护状态进行筛选。
- **可用性健康检查**：定时对已收录链接发起 HEAD 请求，标记响应状态码与超时记录，提供不可用链接的自动降级提示。
- **只读只写 API 接口**：提供 RESTful 风格的查询接口，支持按标签、分类、状态码、更新时间排序输出 JSON 或 CSV 格式数据。
- **可嵌入的挂件组件**：生成一段纯 JavaScript 代码，允许用户将指定分类下的链接列表以卡片或表格形式嵌入个人博客或项目文档页。
- **变更日志与审计追踪**：记录每次链接的新增、删除、状态变更操作，保留操作时间、操作来源 IP 与旧值，便于追溯。
- **全文检索与模糊匹配**：基于简单倒排索引实现域名、标题、描述字段的模糊搜索，支持中文分词与拼音首字母缩写匹配。

## 应用场景

- **技术文档维护者整理参考链接**：当编写框架或库的使用文档时，需要频繁引用官方文档、社区教程、API 参考站。NexusIndex 可将这些外链集中管理，并在文档中通过 API 动态拉取最新可用列表，避免文档内硬编码链接失效。
- **数据采集工程师构建种子库**：在进行分布式爬虫或网站镜像项目时，需要一批稳定的初始种子 URL。NexusIndex 提供标签筛选与健康检查结果，帮助工程师排除高延迟或频繁跳转的链接，提升采集任务成功率。
- **开源项目 README 外链规范化**：大型开源项目通常会在 README 中列出多个相关资源站。NexusIndex 可作为子模块收录这些链接，并提供统一的状态徽章，让用户直接看到每个链接当前是否可访问。
- **研究机构保存文献外链快照**：学术研究过程中积累的大量外部数据源、工具站或数据集页面可能随时迁移。NexusIndex 的记录与审计功能可辅助研究者记录链接变化轨迹，便于在论文修订时更新引用。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，需提前安装 Git 与 Node.js 18+。

```bash
# 1. 克隆仓库
git clone https://github.com/nexus-index/nexusindex.git
cd nexusindex

# 2. 安装依赖
npm install

# 3. 启动开发服务器（默认端口 3000）
npm run dev
```

启动后访问 http://localhost:3000 即可看到本地实例。生产环境部署请参考 `docs/deployment.md` 使用 PM2 或 Docker 运行。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Node.js | 18.17.0 或更高 | 运行时环境，使用原生 fetch 与 crypto 模块 |
| npm | 9.0.0 或更高 | 包管理工具，用于安装依赖与运行脚本 |
| SQLite3 | 系统自带或 npm 安装 | 默认嵌入式数据库，存储链接与标签数据 |
| Redis (可选) | 7.0.0 或更高 | 用于缓存健康检查结果与 API 响应，提升并发性能 |
| Nginx (反向代理推荐) | 1.24.0 或更高 | 生产环境前端代理，处理静态资源与 SSL 终止 |
| PM2 (生产推荐) | 5.0.0 或更高 | Node.js 进程守护，支持自动重启与日志轮转 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|---|---|---|
| 用户手册 | `docs/user-guide/` | 如何导入链接、创建分类、查看健康状态、使用搜索功能 |
| API 参考 | `docs/api-reference/` | 各接口的请求参数、响应结构、错误码与速率限制策略 |
| 运维指南 | `docs/operations/` | 如何配置 Redis 连接、调整健康检查间隔、备份 SQLite 数据库 |
| 贡献规范 | `docs/contributing/` | 代码风格检查、提交信息格式、PR 流程与测试用例编写要求 |
| 设计说明 | `docs/design/` | 数据库表结构设计、标签索引算法、健康检查超时退避策略 |

## 资源列表

以下链接为 NexusIndex 当前收录的外部资源清单，按协议与域名类型分组。所有链接均保持用户原始提交格式，未做任何规范化修改。

### 影视与字幕相关资源

- <code>lalalazhongwendianshijub.org.cn</code>
- <code>yinghuadongmanguanfangbanb.org.cn</code>
- <code>zhongwenzimuyongjiuzaixianb.org.cn</code>
- <code>mianfeizhuijuwangzhanb.org.cn</code>
- <code>gaoqingzhongwenzimub.org.cn</code>
- <code>zaixianbofangnidongdeb.org.cn</code>
- <code>zhongwenzimuzaixianmianfeikanc.org.cn</code>

## 项目结构

```
nexusindex/
├── src/
│   ├── api/                      # RESTful 路由定义与请求校验
│   ├── core/                     # 核心逻辑：链接导入、去重、状态机
│   ├── services/                 # 外部服务集成：健康检查、缓存、日志
│   ├── models/                   # SQLite 数据模型与迁移脚本
│   ├── utils/                    # 通用工具：字符串处理、日期格式化、URL 解析
│   └── worker/                   # 后台任务：定时健康检查、审计清理
├── tests/                        # 单元测试与集成测试用例
├── docs/                         # 完整文档，包含用户手册与 API 参考
├── public/                       # 静态资源，仅用于内置管理面板
├── scripts/                      # 运维脚本：备份、迁移、种子数据填充
├── config/                       # 环境配置：开发、测试、生产三套
├── .github/                      # ISSUE 模板与 PR 校验工作流
├── package.json                  # 项目依赖与脚本入口
└── README.md                     # 项目说明文件（当前文档）
```

## 贡献指南

1. 在 GitHub 上 Fork 本仓库，并基于 `develop` 分支创建您的功能分支，分支命名建议采用 `feat/` 或 `fix/` 前缀。
2. 提交代码前请运行 `npm run lint` 与 `npm run test` 确保风格合规且所有测试通过。新增功能需附带对应单元测试。
3. 编写或更新文档时，请将对应 Markdown 文件放在 `docs/` 目录下，并同步更新 `docs/README.md` 中的目录索引。
4. 提交 PR 时请填写完整模板，描述修改动机、影响范围以及是否破坏向后兼容。PR 至少需要一名维护者批准。
5. 若您计划新增外部资源链接，请通过内置管理面板或 API 提交，而非直接修改代码仓库中的静态列表。

## 常见问题

**Q：NexusIndex 是否存储第三方内容的副本或缓存？**  
A：不存储。系统仅记录 URL、标题、描述、分类标签以及最后一次健康检查的响应状态。所有内容访问均需用户自行通过原始链接进行，系统不代理、不缓存任何响应体。

**Q：健康检查结果多久更新一次？**  
A：默认每 24 小时对所有已收录链接执行一次异步检查。对于连续三次检查失败的链接，系统会将其标记为“不稳定”并在 API 返回结果中附加警告标识。检查间隔可通过环境变量 `CHECK_INTERVAL_HOURS` 调整。

**Q：如何迁移或备份我的链接数据？**  
A：SQLite 数据库文件位于 `data/nexusindex.db`，您可以直接复制该文件进行备份。恢复时替换文件后重启服务即可。若需导出为 CSV 或 JSON，可使用内置命令 `npm run export -- --format=csv`。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-24 22:42:05
