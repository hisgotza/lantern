# Terminus Hub

Terminus Hub 是一个面向技术文档译者、开源社区运营者以及多语言内容创作者的轻量级外链资源聚合与导航系统。该项目并非传统的爬虫或采集工具，而是一个基于静态标记与人工筛选逻辑的“高质量信息源索引库”，旨在解决技术内容生产过程中外链失效、来源不可考、官方入口分散等痛点问题。目标用户包括开源项目维护者、技术博主、本地化协调员以及需要长期跟踪特定领域官方动态的研究人员。通过将大量分布在不同域名下的垂直领域资源（如影视术语库、多语言字幕源、系列作品时间线等）进行结构化整理，Terminus Hub 能够帮助用户在一个统一的目录体系下快速定位权威发布入口，减少信息检索过程中的噪声与安全风险。

## 功能概览

- **多源链接统一看板**：将分散在不同顶级域名下的内容发布入口，按语种、题材与发布批次进行扁平化归类，并提供直接的原始地址引用，便于用户手动确认内容状态。
- **发布批次追踪系统**：内置批次管理逻辑，当前为第 68/120 批，允许协作者通过 Markdown 清单记录每次新增或下线的资源链接，形成可审计的外链变更历史。
- **只读型资源快照**：不缓存或代理任何外部内容，仅记录 URL 原文，确保项目本身不涉及版权内容存储，同时降低维护方的法律合规风险。
- **结构化的文档导航**：通过固定章节（快速开始、安装要求、项目结构等）将项目自身的运维文档与外部资源列表隔离，便于新贡献者理解项目组织方式。
- **纯静态部署兼容**：所有页面内容基于 Markdown 渲染，无需数据库或动态后端，可无缝部署在 GitHub Pages、Cloudflare Pages 或任何支持静态托管的服务上。
- **URL 原文保真机制**：严格保留用户提交的原始 URL 格式（包括协议前缀、域名大小写及路径结尾符号），避免因自动格式化导致链接无法访问。
- **人工校验标记支持**：允许在资源列表旁附加状态注释（如“有效”、“疑似迁移”、“需要登录”），辅助团队进行周期性链接健康度检查。

## 应用场景

- **技术文档本地化团队的外链管理**：当翻译一份涉及多个外部参考站点的技术白皮书时，团队可使用 Terminus Hub 集中存放原始外链，避免在翻译稿内直接嵌入冗长地址，同时便于审校人员逐一验证引用来源。
- **开源项目官网的资源推荐页构建**：开源社区常需要在官网列出相关生态项目或合作伙伴入口，通过本项目的目录结构，可以快速生成一个合规的外链披露页面，满足基金会对于外部链接的审计要求。
- **个人知识库的种子链接聚合**：研究者或内容创作者可将长期关注的特定领域发布源（例如某系列动画的官方更新站、某语种字幕首发平台）纳入本系统，作为个人知识图谱的入口层，减少日常查找的重复劳动。
- **社区运营的周期性链接巡检**：运营人员可依据项目生成的批次清单，每月导出所有 URL 列表，使用自动化脚本进行连通性测试，并及时在注释字段中标记失效链接，提升社区资源质量。

## 快速开始

以下指令适用于 Linux / macOS / Windows WSL 环境，要求系统已安装 Git 与 Node.js 18.x 及以上版本。

```bash
# 克隆项目仓库至本地
git clone https://github.com/terminus-hub/terminus-hub.git
cd terminus-hub

# 安装依赖（项目使用 pnpm 作为包管理器，若未安装请先执行 npm install -g pnpm）
pnpm install

# 启动本地开发服务器，默认监听端口 3000
pnpm run dev
```

启动成功后，访问控制台输出的本地地址（通常为 http://localhost:3000）即可查看当前资源列表与文档导航。如需构建生产版本，请执行 `pnpm run build`，输出目录为 `dist`。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x 或 20.x LTS | 运行时环境，用于执行构建脚本与本地开发服务器 |
| pnpm | 8.x 或更高版本 | 包管理器，用于依赖安装与工作区管理 |
| Git | 2.25+ | 版本控制工具，用于克隆仓库及提交变更 |
| Markdown 解析器 | 项目内置（marked） | 用于将 README 及资源文档渲染为 HTML，无需单独安装 |
| 操作系统 | Linux / macOS / Windows (WSL2) | 开发与部署环境，生产构建不依赖特定 OS |
| 浏览器 | 现代浏览器（Chrome 110+ / Firefox 110+） | 仅用于本地预览，生产访问无额外要求 |
| 网络连通性 | 可访问公网 | 用于初次克隆及拉取依赖包，后续运行无需持续联网 |
| 磁盘空间 | 至少 50 MB | 用于存放源码、依赖及构建产物 |
| 权限 | 对项目目录具有读写权限 | 用于生成构建文件及写入日志 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 项目入门 | `/docs/quick-start.md` | 如何在一分钟内完成本地环境搭建并看到资源列表？ |
| 资源维护 | `/docs/resource-lifecycle.md` | 如何新增、更新或移除一个外链条目？批次号如何递增？ |
| 结构设计 | `/docs/architecture.md` | 项目的目录划分逻辑是什么？静态资源如何组织？ |
| 贡献规范 | `/CONTRIBUTING.md` | 提交 Pull Request 时需要遵循哪些格式与测试要求？ |
| 部署指南 | `/docs/deployment.md` | 支持哪些托管平台？环境变量如何配置？ |

## 资源列表

本批次（第 68/120 批）收录以下原始发布入口，所有链接均按照用户提供内容原样呈现，未做任何格式修正或跳转改写。

### 影视与动画相关资源

<code>rimanzaixianguankanf.org.cn</code>

<code>rihanzaixianmianfeishipinf.org.cn</code>

<code>zhongwenzimumianfeibofangf.org.cn</code>

<code>renqixiliezhongwenzimuf.org.cn</code>

<code>wuyefulizhibof.org.cn</code>

<code>lalalazhongwendianshijuf.org.cn</code>

<code>yinghuadongmanguanfangbanf.org.cn</code>

## 项目结构

项目采用分层目录设计，将核心逻辑、文档、资源清单与构建配置严格隔离。以下为关键目录与文件说明：

```
terminus-hub/
├── src/                                 # 应用源代码主目录
│   ├── core/                            # 核心工具函数（URL 校验、批次管理）
│   │   ├── validator.js                 # URL 格式校验与域名白名单检查
│   │   └── batch.js                     # 批次号生成与递增逻辑
│   ├── data/                            # 资源数据存储层
│   │   ├── batches/                     # 按批次存放的 JSON 资源清单
│   │   │   └── batch-68.json            # 第 68 批资源列表（含原始 URL 及状态）
│   │   └── schema/                      # 资源条目 JSON Schema 定义
│   │       └── resource.schema.json     # 规定每个条目必须包含 url, source, status 字段
│   ├── pages/                           # 页面渲染组件（基于 Vite）
│   │   ├── Home.vue                     # 首页，展示批次概览与最新资源
│   │   └── ResourceList.vue             # 全量资源表格视图，支持按域名筛选
│   ├── styles/                          # 全局样式与主题变量
│   │   ├── variables.css                # CSS 自定义属性（色板、间距）
│   │   └── reset.css                    # 浏览器样式重置
│   └── utils/                           # 通用辅助函数
│       ├── format.js                    # 日期格式化与链接文本截断
│       └── storage.js                   # localStorage 读写封装（用于用户偏好）
├── docs/                                # 项目文档（面向贡献者与维护者）
│   ├── quick-start.md                   # 快速开始指南（含故障排查）
│   ├── resource-lifecycle.md            # 资源增删改查流程与批次规范
│   ├── architecture.md                  # 架构设计决策记录（ADR）
│   └── deployment.md                    # 各平台部署配置示例（Netlify / Vercel / Cloudflare）
├── scripts/                             # 运维与辅助脚本
│   ├── validate-urls.js                 # 遍历所有批次 JSON，校验 URL 可访问性（HEAD 请求）
│   └── generate-sitemap.js              # 根据资源列表生成 sitemap.xml
├── tests/                               # 单元测试与集成测试
│   ├── unit/                            # 基于 Vitest 的工具函数测试
│   │   └── validator.spec.js            # URL 校验逻辑的边界用例
│   └── e2e/                             # 基于 Playwright 的页面交互测试
│       └── navigation.spec.js           # 测试资源列表的筛选与排序功能
├── .github/                             # GitHub 社区配置
│   ├── ISSUE_TEMPLATE/                  # 资源失效报告与新增建议模板
│   └── workflows/                       # CI 流水线（构建、链接检查、部署）
├── package.json                         # 项目依赖清单与脚本入口
├── pnpm-lock.yaml                       # 依赖锁定文件（确保可重现构建）
├── vite.config.js                       # 构建工具配置（别名、代理、插件）
├── tsconfig.json                        # TypeScript 编译选项（用于类型检查）
└── README.md                            # 当前文档（项目入口说明）
```

## 贡献指南

我们欢迎所有形式的贡献，包括但不限于新增资源链接、修复文档错误、提出功能建议或改进代码质量。请遵循以下步骤确保协作顺畅：

1.  **查阅现有议题与 Pull Request**：在提交新工作前，请先浏览 GitHub Issues 与 Pull Requests，确认无人正在处理相同内容，避免重复劳动。
2.  **创建功能分支并遵守命名规范**：从 `main` 分支切出新的工作分支，名称格式为 `feature/简短描述` 或 `fix/问题编号`，例如 `feature/add-batch-69`。
3.  **更新资源列表与批次文件**：若涉及资源新增或删除，请同步修改 `src/data/batches/` 下对应的 JSON 文件，并更新批次号（遵循 `batch.js` 中的递增规则）。同时在本 README 的“资源列表”章节中同步变更，保持一致性。
4.  **运行本地校验脚本**：在提交前执行 `pnpm run validate`，该脚本将检查所有 URL 的格式合法性以及 JSON 结构的完整性。若有测试失败，请修正后再行提交。
5.  **发起 Pull Request 并描述变更**：PR 标题应简明扼要，正文需包含变更动机、测试结果以及是否涉及破坏性改动。至少一位项目维护者审阅通过后，即可合并入主分支。

## 常见问题

**Q：项目中的 URL 为什么不做成可点击的超链接？直接复制到浏览器访问太麻烦了。**

A：出于安全与合规考量，项目刻意避免自动解析或重写用户提供的原始 URL。我们无法保证所有第三方域名的内容长期合法、安全或未发生劫持。保留纯文本形式可以强制用户手动确认目标地址，降低因自动跳转带来的钓鱼风险。同时，这也符合“只读索引”的设计定位，项目本身不承担代理或中转职责。

**Q：我提交的 URL 被校验脚本拒绝了，提示“格式不通过”或“域名未备案”，怎么办？**

A：校验脚本仅执行基础格式检查（如是否包含非法字符、协议头是否与预设一致）。如果您的 URL 确实可以正常访问但被误判，请先检查是否包含多余的空格或特殊符号。若确认无误，请在 GitHub Issues 中提交“校验例外请求”，并附上截图证明该链接可正常打开，维护团队将评估是否调整校验规则。

**Q：项目会主动爬取或缓存这些外链指向的内容吗？**

A：不会。Terminus Hub 在任何情况下都不会发起对第三方域名的自动内容抓取、缓存或镜像存储。项目仅维护 URL 字符串本身。所有的连通性检查（如 `validate-urls.js` 脚本）均基于可选的 HEAD 请求，且默认关闭，需要用户手动执行。这一设计保证项目完全符合《信息网络传播权保护条例》关于“仅提供链接服务”的免责条款。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-24 22:42:01
