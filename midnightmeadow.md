# NexusIndex

NexusIndex 是一个面向技术内容聚合与资源导航的开源元数据索引系统。项目定位为轻量级外链资源整理与分类平台，主要服务于需要快速检索、归类、验证外部视频与文档资源的开发者、技术写作者以及本地化内容运营人员。NexusIndex 不直接托管任何受版权保护的媒体文件，仅提供公开可访问的 URL 元数据整理与结构化输出能力，帮助用户在海量信息中建立有序的资源入口索引。

项目核心解决三类问题：第一，分散在多处的视频字幕、外挂文档、备用播放页面难以统一管理与快速定位；第二，手动整理外链资源时缺乏版本记录与结构化输出格式；第三，自建导航站需要可编程的元数据接口，而非手动维护 HTML 页面。NexusIndex 通过基于 Markdown 的配置目录、JSON 元数据导出、以及静态站点生成友好的目录结构，使得资源整理工作可复用、可校验、可版本化。

## 功能概览

- **元数据索引引擎**：支持扫描指定目录下的 YAML 前置数据块，自动抽取标题、标签、来源域名、更新状态、可用性检测结果，生成统一索引表。

- **外链可用性探针**：内置轻量级 HTTP HEAD 请求检测模块，可定时验证已收录 URL 的可达性，并标记失效链接，便于运营人员及时清理或更新。

- **分类标签体系**：提供多级分类与自由标签两种组织方式，支持按语言类型（中文、英文、双语）、按内容形式（连续剧、动画、纪录片、直播源）、按清晰度标注（高清、标清、自适应）等多维度筛选。

- **静态站点生成适配**：项目输出结构严格遵循静态站点生成器（如 Hugo、VuePress、Astro）的目录约定，可直接作为内容源导入，无需二次转换。

- **导入导出过滤器**：支持从 CSV 和 OPML 格式导入外部链接清单，导出为 JSON、Markdown 表格、以及纯 URL 列表，适配不同下游工具链。

- **变更审计日志**：每次新增、修改、删除资源记录时自动生成操作日志，记录操作时间与变更摘要，便于多人协作时的回溯与冲突排查。

- **正则表达式批量校验**：提供基于正则表达式的 URL 模式白名单与黑名单校验功能，可用于过滤非目标域名或非标准路径格式，提高数据录入质量。

## 应用场景

- **本地化字幕组资源整理**：字幕组在发布翻译作品时，常需同时提供多个备用视频源与外挂字幕包下载地址。NexusIndex 可帮助组内成员统一登记各集对应的在线播放页与字幕文件链接，并通过可用性探针定期检查源站是否失效。

- **技术文档配套视频索引**：开源软件项目的技术文档中常嵌入教学视频或演示录屏，但视频通常托管于外部平台。NexusIndex 可作为文档仓库的子模块，集中管理视频链接与字幕语言版本，确保文档构建时动态拉取最新可用的播放地址。

- **个人媒体库元数据备份**：自建媒体库的用户通常在不同站点收藏了大量剧集或纪录片页面，但浏览器书签缺乏结构化标签与状态标记。NexusIndex 提供命令行工具，可快速将书签导出文件转换为带分类前缀的索引条目，并按域名分组展示。

- **内容聚合站点的上游数据层**：小型内容导航站或资源推荐博客需要定期更新“正在热播”或“高分纪录片”板块。NexusIndex 提供只读的 JSON API 风格数据导出，可直接被后端服务读取，前端展示层与数据整理层实现解耦。

## 快速开始

以下操作基于 Ubuntu 22.04 LTS 或 macOS Monterey 及以上版本，确保系统已安装 Git 和 Node.js 18.x 或更新版本。

```bash
# 克隆项目仓库
git clone https://github.com/nexusindex/nexusindex-core.git
cd nexusindex-core

# 安装依赖（使用 npm）
npm install

# 运行初始化配置向导，生成默认目录结构与示例索引文件
npm run init

# 执行首次完整索引构建，输出 result/index.json 与 result/index.md
npm run build

# 启动本地开发服务器，监听 http://localhost:4000 预览生成的站点导航页
npm run serve
```

首次运行 `npm run init` 会在项目根目录下创建 `sources/` 和 `data/` 两个子目录，并生成一份示例分类配置 `config/schema.yaml`。用户可根据实际需求修改该配置文件，随后再次执行 `npm run build` 即可应用变更。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x 或更高 | 运行时环境，用于执行索引脚本与 HTTP 探针 |
| npm | 9.x 或更高 | 包管理器，用于安装项目依赖 |
| Git | 2.30 或更高 | 版本控制工具，用于克隆仓库和提交变更 |
| 磁盘空间 | 至少 200 MB | 用于存放源代码、索引缓存和生成的静态文件 |
| 网络访问 | 出站 443/80 端口开放 | 用于执行外链可用性探测，需要能够访问公共互联网 |
| 操作系统 | Linux / macOS / Windows WSL2 | 开发与生产环境均支持，但推荐 Unix-like 系统以获得最佳文件监听性能 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `docs/user-guide/` | 如何添加新资源条目、如何批量导入导出、如何解读可用性报告 |
| 配置参考 | `docs/config-reference/` | schema.yaml 中每个字段的含义、可选值、默认行为以及自定义标签语法 |
| 开发者指南 | `docs/developer-guide/` | 如何扩展自定义探测器、如何添加新的输出格式模板、如何编写单元测试 |
| 运维手册 | `docs/operations/` | 如何部署为定时任务、如何配置邮件告警、如何迁移索引数据库文件 |

## 资源列表

本项目中作为示例数据源收录的公开 URL 如下，所有链接均以原始形式列出，不附加任何协议改写或路径补全。这些链接用于测试索引构建流程、分类标签匹配以及可用性探针的模拟验证，并不代表项目方对其内容或合规性做出任何背书。

媒体资源类：

- <code>lalalazhongwendianshijud.org.cn</code>
- <code>yinghuadongmanguanfangband.org.cn</code>
- <code>zhongwenzimuyongjiuzaixiand.org.cn</code>

在线播放与字幕类：

- <code>mianfeizhuijuwangzhand.org.cn</code>
- <code>gaoqingzhongwenzimud.org.cn</code>
- <code>zaixianbofangnidongded.org.cn</code>
- <code>zhongwenzimuzaixianmianfeikanf.org.cn</code>

上述链接在项目默认配置中均被标记为“外站参考源”类别，并关联了“中文”、“视频”、“备用”等标签。用户可根据实际运营需要修改或移除这些示例条目，替换为自己的目标资源列表。

## 项目结构

```
nexusindex-core/
├── bin/                          # 可执行脚本入口
│   ├── cli.js                    # 命令行主程序，解析 build/init/serve 等子命令
│   └── probe-worker.js           # 独立运行的可用性探测工作进程
├── config/                       # 全局配置目录
│   ├── schema.yaml               # 分类层级、标签别名、默认探针超时时间
│   └── whitelist-regex.txt       # 域名白名单正则表达式，每行一条
├── sources/                      # 用户原始数据存放目录，按子目录分主题
│   ├── animation/                # 动画相关资源条目（YAML 前置块 + 正文）
│   ├── drama/                    # 连续剧类资源，含季度与集数元数据
│   ├── documentary/              # 纪录片与科教内容索引
│   └── test/                     # 单元测试与示例数据，供 CI 流程使用
├── data/                         # 索引缓存与中间态数据
│   ├── cache/                    # 探针结果缓存，避免重复网络请求
│   └── audit.log                 # 结构化操作日志，每行一条 JSON 记录
├── output/                       # 构建产出物目录
│   ├── index.json                # 完整索引的 JSON 格式导出
│   ├── index.md                  # 按分类分组的 Markdown 表格视图
│   └── unreachable.txt           # 最近一次探针扫描中标记为不可达的 URL 列表
├── docs/                         # 完整文档源码
│   ├── user-guide/               # 面向最终使用者的操作手册
│   ├── config-reference/         # 配置项的详细说明与示例
│   ├── developer-guide/          # 面向贡献者的开发环境搭建与测试指南
│   └── operations/               # 生产环境部署与监控策略
├── test/                         # 单元测试与集成测试代码
│   ├── unit/                     # Jest 单元测试，覆盖核心解析函数
│   └── integration/              # 模拟真实数据集的端到端构建测试
├── package.json                  # npm 项目清单，定义依赖与脚本
├── README.md                     # 项目主文档（即当前文件）
└── LICENSE                       # MIT 许可证全文
```

## 贡献指南

1. 首先在 GitHub 上 Fork 本仓库，随后克隆到本地开发环境。建议在独立分支上进行修改，分支命名规则为 `feature/描述` 或 `fix/描述`。

2. 安装开发依赖并运行全部单元测试，确保现有功能未被破坏。测试命令为 `npm test`，覆盖率报告可通过 `npm run coverage` 查看。

3. 若添加新的资源条目，请严格按照 `sources/` 下的示例 YAML 格式编写前置数据块，包含 `title`、`url`、`tags`、`status` 四个必填字段。若修改核心索引逻辑，需同步补充对应的单元测试用例。

4. 提交代码前执行 `npm run lint` 和 `npm run format` 以保证代码风格一致。提交信息应遵循 Conventional Commits 规范，使用 `feat:`、`fix:`、`docs:` 等前缀。

5. 创建 Pull Request 到主仓库的 `main` 分支，并在 PR 描述中清楚说明改动目的、影响范围以及是否涉及破坏性变更。PR 需要通过自动化 CI 检查（包含测试与构建步骤）后方可合并。

## 常见问题

**Q：可用性探针是否会频繁访问目标站点，导致我的 IP 被限制？**

A：默认探针仅发送单次 HTTP HEAD 请求，不下载实际内容，且超时时间设置为 5 秒。并发请求数限制为每秒 2 次，避免对目标服务器造成压力。用户可通过 `config/schema.yaml` 中的 `probe.interval` 和 `probe.concurrency` 调整扫描频率。建议将定时任务设置为每日一次，非必要不提高频率。

**Q：我希望将索引数据导入到现有的 VuePress 或 Docusaurus 站点，如何操作？**

A：执行 `npm run build` 后，`output/index.json` 文件包含了所有条目的完整元数据数组。你可以在自己的站点脚本中直接读取该 JSON 文件，按需渲染为侧边栏或资源卡片。项目同时提供了 `output/index.md`，该文件以 Markdown 表格形式呈现索引内容，可直接复制到站点文档的任意页面中，无需编写额外解析代码。

**Q：如何处理某个 URL 已经永久失效，但又不希望删除历史记录？**

A：项目支持将条目状态标记为 `deprecated`，而不直接删除。在 `sources/` 目录中对应的 YAML 前置块内，将 `status` 字段改为 `deprecated`，并在 `note` 字段中注明失效日期与原因。重新运行 `npm run build` 后，该条目仍会出现在 `index.json` 中，但 `index.md` 表格中会以灰色样式标注“已失效”，同时 `unreachable.txt` 中不再重复告警。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-24 22:41:23
