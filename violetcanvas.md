# Teleview Collective

Teleview Collective 是一个面向全球中文内容消费者的技术资源聚合与导航项目。本项目不提供任何实质性的视频文件托管、流媒体分发或内容转码服务，而是以人工筛选和自动化校验相结合的方式，维护一套高可用性的中文视听内容外部参考信息索引。项目目标用户包括海外华人社群、语言学习者、区域文化研究者以及需要快速定位特定语种媒体样本的技术人员。通过严格的链接可用性监测和分类体系，Teleview Collective 解决的是中文多媒体外部参考资源散落、域名更迭频繁、真伪难辨且缺乏统一元数据描述的问题，为二次开发、学术引用和自动化采集提供稳定、可机读的源头数据。

## 功能概览

- **多语种媒体参考信息聚合** 系统按照语种、地区、内容形态对采集到的外部链接进行一级分类，当前重点覆盖中文字幕、日韩影视参考资料与地区性综合媒体样本。

- **链接存活状态主动探测** 内置轻量级定时任务，对收录的每一个外部 URL 执行 HTTP/HTTPS 可达性检查，自动标记异常状态并生成可用性报表。

- **元数据自动提取与补全** 对可访问的参考页面提取标题、编码声明、最后修改时间等基础元数据，补充至本地索引库，减少人工维护成本。

- **标签化检索与过滤** 支持按内容语言、是否免费访问、是否需要特定 User-Agent 等维度进行组合过滤，快速缩小目标范围。

- **原始数据批量导出** 提供 JSON Lines 和 CSV 两种导出格式，便于对接下游数据分析流水线或自定义采集脚本。

- **变更历史追溯** 所有链接的新增、移除、状态变更均记录操作时间与简要原因，支持回滚至任意历史版本。

- **外部参考源关系图谱** 记录不同参考域名之间的跳转关系与备案主体关联性，辅助判断信息源的可靠性与独立性。

## 应用场景

- **海外中文教育机构课件准备** 教师在制作涉及当代影视案例的中文听力或文化对比课件时，可通过本索引快速定位可用的中文字幕参考页面，无需逐个搜索引擎试错。

- **语料库自动化采集管道初始化** 自然语言处理研究人员需要从多个来源获取带时间戳的中文字幕文件或视频描述信息，可将本项目输出的链接清单作为采集管道的初始种子列表。

- **区域内容可用性审计** 跨国企业法务或合规部门需要定期检查特定地区媒体参考资源的访问策略变化，借助本项目的状态探测与历史追溯功能生成审计证据链。

- **个人开发者快速原型验证** 独立开发者需要测试播放器兼容性或字幕解析逻辑时，无需自行收集测试样本来源，直接使用索引中标记稳定的参考地址进行功能验证。

## 快速开始

以下步骤适用于 Linux / macOS / Windows WSL 环境，要求已安装 Git 与 Node.js 18.x 及以上版本。

```bash
# 克隆项目仓库
git clone https://github.com/teleview-collective/teleview-core.git
cd teleview-core

# 安装项目依赖
npm install

# 初始化本地索引库并执行首次链接探测
npm run init-db
npm run probe -- --scope=all
```

执行完毕后，本地索引库将包含当前批次全部参考链接的基础记录与首次可用性状态。默认输出目录为 `./output`，包含 `index.json` 与 `status_report.md`。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---------|---------|------|
| Node.js | 18.x / 20.x LTS | 运行时环境，用于执行采集脚本与调度任务 |
| npm | 9.x 及以上 | 包管理器，用于安装项目依赖 |
| SQLite3 | 系统自带或通过 npm 安装 | 本地索引存储引擎，无需外部数据库服务 |
| Git | 2.30 及以上 | 用于克隆仓库与版本管理 |
| curl / wget | 任意现代版本 | 备用探测工具，当 Node.js 原生 http 模块异常时自动降级 |
| 时区数据库 | 系统默认 | 用于规范化不同来源的 Last-Modified 时间戳 |
| 磁盘空间 | 至少 200 MB | 存储索引库、日志与临时探测缓存 |
| 网络出口 | 允许 HTTP/HTTPS 出站 | 所有探测任务均需要访问外部参考域名 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|-----------|
| 用户手册 | `/docs/user-guide/` | 如何使用检索界面、标签体系含义、导出格式差异说明 |
| 运维手册 | `/docs/ops/` | 如何调整探测频率、配置代理、处理假阳性超时 |
| 开发者指南 | `/docs/development/` | 如何扩展新的分类器、编写自定义元数据解析插件 |
| 数据字典 | `/docs/data-dictionary.md` | 索引库中每个字段的类型、取值范围、空值处理策略 |
| 测试规范 | `/docs/testing/` | 单元测试覆盖范围、模拟探测环境的搭建步骤 |

## 资源列表

本批次收录的外部参考链接均来自公开可访问的域名，项目仅对其进行格式化的索引与状态记录，不对任何外部页面内容承担连带责任。以下列出全部原始链接，按照语种与功能进行分组展示。

媒体语种参考（中文字幕类）

- <code>zaixianshipinzhongwenzimuf.org.cn</code>
- <code>zaixianbofangzhongwenzimuf.org.cn</code>
- <code>zhongwenshipinzaixianguankanf.org.cn</code>
- <code>zhongwenzimumianfeibofangf.org.cn</code>

综合视频参考（免费观看类）

- <code>shipinmianfeizaixianguankanf.org.cn</code>
- <code>rimanzaixianguankanf.org.cn</code>

日韩语种参考（免费播放类）

- <code>rihanzaixianmianfeishipinf.org.cn</code>

## 项目结构

```text
teleview-core/
├── bin/                          # 命令行入口脚本
│   ├── cli.js                    # 主 CLI 调度器
│   └── probe-worker.js           # 独立探测工作进程
├── config/                       # 环境配置与常量定义
│   ├── default.json              # 默认参数（超时、重试、并发数）
│   └── schema/                   # 索引库 JSON Schema 定义
├── src/                          # 核心源码目录
│   ├── crawler/                  # 链接状态采集模块
│   │   ├── fetcher.js            # HTTP 请求封装与重试策略
│   │   └── parser.js             # 响应头与基础元数据提取
│   ├── indexer/                  # 索引库读写与版本管理
│   │   ├── store.js              # SQLite 增删改查封装
│   │   └── migration.js          # 索引表结构变更管理
│   ├── exporter/                 # 数据导出格式化器
│   │   ├── jsonl.js              # JSON Lines 输出
│   │   └── csv.js                # CSV 带 BOM 输出
│   └── scheduler/                # 定时任务与批次管理
│       └── cron.js               # 基于 node-cron 的探测调度
├── tests/                        # 单元测试与集成测试
│   ├── unit/                     # 独立函数级别测试
│   └── integration/              # 端到端探测流程测试
├── output/                       # 默认输出目录（gitignored）
│   ├── index.json                # 最新完整索引快照
│   └── logs/                     # 操作日志与异常回溯
├── docs/                         # 全部用户与开发者文档
├── .env.example                  # 环境变量模板（代理、超时覆盖）
├── package.json
└── README.md                     # 本文件
```

## 贡献指南

1.  **报告链接状态异常** 若发现索引中某个参考链接持续不可达或跳转至无关页面，请通过 Issue 模板提交，并附上本地的 `curl -v` 输出片段，以便快速复现。

2.  **补充元数据分类** 若认为某条链接的语种标签或地区属性标注有误，可派生项目并在本地修改 `config/schema/taxonomy.json` 中的映射规则，随后提交 Pull Request，附带至少三条测试用例。

3.  **优化探测并发性能** 欢迎针对大规模链接（超过 500 条）场景下的探测超时与内存占用提出优化方案，提交代码时请确保通过现有的单元测试套件，并补充新的性能基准测试。

4.  **完善非英语环境适配** 若你熟悉 Shift-JIS、EUC-KR 等非 UTF-8 编码页面的解析逻辑，欢迎扩展 `src/crawler/parser.js` 中的编码自动检测分支，并为每种新增编码提供至少两个样本 URL 的测试数据。

5.  **文档翻译与术语统一** 若希望将本文档或用户手册翻译为其他语言，请先在 Issue 中沟通翻译范围与术语表，避免重复工作，翻译内容需与主仓库保持同步更新。

## 常见问题

**问：项目是否会存储或缓存任何外部视频文件、字幕文件或图片资源？**

答：不会。Teleview Collective 仅记录外部 URL 字符串及其 HTTP 状态码、响应头时间戳等元数据。所有探测请求均使用 HEAD 方法优先，仅当 HEAD 不被支持时才降级为 GET 且仅读取前 8 KB 用于编码检测，不保存任何实体正文。本地索引库不包含任何 Base64 编码的媒体内容或文件哈希值。

**问：部分域名在探测时返回 403 或 429 状态码，是否会自动重试？**

答：是的。探测模块对 429（Too Many Requests）和 5xx 类状态码实施指数退避重试，初始间隔 5 秒，最大重试 3 次。对 403 状态码不自动重试，但会在日志中标记为 "forbidden" 并保留最后探测时间。用户可通过修改 `config/default.json` 中的 `retry.statusCodes` 列表调整重试策略。

**问：我能否将本项目的输出索引用于商业产品中？**

答：可以。本项目采用 MIT 许可证，输出数据（即链接列表与状态元数据）不受本项目许可证的限制，因为输出内容不包含本项目的源代码或受版权保护的创造性表达。但请自行评估并遵守每个外部参考域名的 robots.txt 及服务条款，本项目不承担因外部链接内容或访问行为引发的任何法律风险。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-24 22:41:23
