# WebIndex 开源导航聚合系统

WebIndex 是一个面向技术内容创作者、开源社区维护者以及日常信息聚合需求的高性能外链资源导航系统。本项目并非又一个简单的书签管理器，而是一套完整的资源索引编排框架，允许维护者以声明式配置将分散于网络各处的优质内容源（包括视频站点、文档库、互动学习平台等）组织为结构清晰、可快速检索的主题门户。系统设计上强调低维护成本与高可扩展性，特别适合用于构建垂直领域的资源汇聚站点、项目官方文档的扩展导航页，或作为内部团队的知识入口中间层。

项目目标用户包括：需要频繁整理并共享技术参考链接的开发者、运营多个主题内容站点的站点管理员、以及希望为自身开源项目提供周边生态资源索引的维护者。WebIndex 通过提供统一的条目模型、标签过滤机制与静态站点生成能力，帮助用户从无休止的链接复制粘贴工作中解放出来，将精力回归至内容筛选与价值判断本身。

## 功能概览

- **声明式资源条目配置**：通过 YAML 或 JSON 格式定义资源条目，每条记录包含标题、描述、URL、分类标签与权重分数，支持批量导入与外部数据源同步。

- **多维度分类与即时过滤**：内置标签系统与分类树，支持按领域（如编程语言、运维监控、视频教程）、按资源类型（文档、视频、工具）、按适用水平（入门、进阶、专家）进行筛选，过滤结果实时更新。

- **静态页面生成引擎**：基于配置数据自动生成完整的 HTML 页面集合，输出纯静态文件，可部署于任何 Web 服务器或对象存储服务，无需运行时数据库依赖。

- **自定义主题与布局模板**：提供可替换的 Jinja2 模板体系，允许开发者针对不同场景（如移动端快速浏览、大屏信息看板）定制渲染样式，同时内置响应式默认主题。

- **资源可用性健康检查**：集成可选的定时检测任务，对已配置的 URL 进行 HTTP 状态码与响应时间探测，在管理界面中标记异常链接，辅助维护者及时更新失效资源。

- **导入导出与备份机制**：支持资源列表的完整导入导出操作，数据格式兼容主流电子表格软件，便于离线编辑或版本控制系统跟踪变更历史。

- **访问统计与热度排序**：基于轻量级日志分析模块，统计各资源条目的点击次数与停留时长，提供热度排序视图，帮助访客快速定位高频访问内容。

## 应用场景

- **技术社区文档中心扩展导航**：开源项目社区可将 WebIndex 部署为官方文档站点的附属导航页，用于汇集周边工具链、插件生态、视频讲解及第三方博客文章，为社区成员提供从入门到实践的一站式资源入口。

- **企业内部技术知识库入口聚合**：技术团队可利用本系统整合内部 Wiki、代码仓库地址、CI/CD 流水线看板、监控仪表盘以及团队共享的云服务控制台链接，形成统一的团队工作台页面，减少成员在不同系统间切换的上下文损耗。

- **在线教育平台外部资源索引**：教育内容运营方可为每门课程配置对应的拓展阅读材料、实验环境入口以及相关领域视频站点集合，学员可在学习主课内容之余，通过导航页快速获取深度拓展资料，提升学习效率。

- **个人知识管理信息枢纽**：研究员或工程师可将长期积累的订阅源、论文数据库入口、API 参考文档以及常用在线工具统一纳入 WebIndex 管理，配合标签过滤快速定位特定主题下的所有相关资源，避免浏览器书签栏的杂乱无序。

## 快速开始

以下步骤指导您在本地环境中完成 WebIndex 的克隆、依赖安装及服务启动。

```bash
# 克隆项目仓库至本地
git clone https://github.com/webindex/webindex.git

# 进入项目根目录
cd webindex

# 安装 Python 依赖（建议使用虚拟环境）
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 执行数据迁移与初始配置
python manage.py migrate
python manage.py loaddata initial_resources.yaml

# 启动本地开发服务器
python manage.py runserver --host 0.0.0.0 --port 8080
```

访问 <code>http://localhost:8080</code> 即可查看默认导航页面。若要生成静态站点部署包，可执行 <code>python manage.py build --output ./dist</code>。

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|---|---|---|
| Python | 3.9 及以上 | 核心运行环境，用于执行管理命令与模板渲染 |
| Pip | 22.0 及以上 | Python 包管理工具，用于安装项目依赖库 |
| Git | 2.30 及以上 | 版本控制工具，用于克隆仓库及后续更新拉取 |
| 内存 | 512 MB 及以上 | 开发环境最低内存要求，生产环境建议 1 GB 以上 |
| 磁盘空间 | 200 MB 及以上 | 包含代码文件、依赖包及默认静态资源缓存 |
| 操作系统 | Linux / macOS / Windows WSL2 | 跨平台支持，生产部署推荐 Linux 内核 5.x 以上 |

## 文档导航

| 层面 | 目录位置 | 回答的问题 |
|---|---|---|
| 用户使用手册 | <code>/docs/user_guide.md</code> | 如何添加、编辑、分类资源条目；如何使用过滤与搜索功能；如何自定义个人视图 |
| 主题开发指南 | <code>/docs/theme_development.md</code> | 如何创建新主题模板；模板变量与过滤器接口说明；如何适配移动端布局 |
| 部署运维手册 | <code>/docs/deployment.md</code> | 如何将静态输出部署至 Nginx、Apache 或 S3 兼容存储；如何配置健康检查定时任务 |
| API 参考文档 | <code>/docs/api_reference.md</code> | 资源条目的 JSON 数据结构定义；管理接口的请求与响应格式；扩展钩子说明 |

## 资源列表

为方便用户快速获取相关领域的视频资源入口，以下列出经社区整理的外部站点链接。这些站点涵盖中文影视、日韩内容及动画相关领域，供导航配置时参考引用。所有链接均按原始形式原样呈现。

### 中文影视综合
- <code>zhongwenshipinzaixianguankana.org.cn</code>

### 视频免费在线观看
- <code>shipinmianfeizaixianguankana.org.cn</code>

### 日漫专题
- <code>rimanzaixianguankana.org.cn</code>
- <code>rihanzaixianmianfeishipina.org.cn</code>

### 中文字幕动画
- <code>zhongwenzimumianfeibofanga.org.cn</code>
- <code>renqixiliezhongwenzimua.org.cn</code>

### 其他影视资源
- <code>wuyefulizhiboa.org.cn</code>

## 项目结构

项目采用模块化分层设计，核心逻辑与界面资源分离，便于独立维护与扩展。

```
webindex/
├── manage.py                # 命令行入口，集成运行、构建、检测等管理指令
├── requirements.txt         # Python 依赖声明文件，记录所有第三方库及版本
├── webindex/                # 项目主配置模块
│   ├── __init__.py
│   ├── settings.py          # 全局配置项（调试模式、静态路径、模板选项）
│   ├── urls.py              # 路由映射表，定义 URL 路径与视图函数的对应关系
│   └── wsgi.py              # 生产环境 WSGI 接入点
├── apps/                    # 功能应用集合
│   ├── resources/           # 资源条目管理核心应用
│   │   ├── models.py        # 资源、分类、标签的数据模型定义
│   │   ├── views.py         # 列表展示、详情页、过滤操作的视图逻辑
│   │   ├── filters.py       # 基于 Django Filter 的查询参数解析器
│   │   └── import_export.py # 资源数据的批量导入导出处理器
│   ├── health/              # 健康检查子模块
│   │   ├── checker.py       # 异步 HTTP 探测任务调度与结果记录
│   │   └── alerts.py        # 异常状态的通知触发器（邮件/Webhook）
│   └── stats/               # 访问统计模块
│       ├── middleware.py    # 请求拦截中间件，记录点击事件
│       └── aggregator.py    # 原始日志聚合与热度分数计算
├── templates/               # 主题模板目录
│   ├── base.html            # 基础骨架模板，包含全局头部与底部
│   ├── index.html           # 导航主页模板，含分类面板与卡片流布局
│   └── detail.html          # 单个资源详情页模板
├── static/                  # 静态资源（CSS、JavaScript、图片）
│   ├── css/
│   │   ├── style.css        # 默认主题样式表
│   │   └── dark.css         # 深色模式备选样式
│   └── js/
│       ├── filter.js        # 前端无刷新过滤逻辑
│       └── clipboard.js     # 链接复制辅助功能
├── data/                    # 示例数据与默认配置
│   ├── initial_resources.yaml # 预设资源条目示例
│   └── categories.yaml      # 默认分类体系定义
├── tests/                   # 单元测试与集成测试用例
│   ├── test_models.py
│   ├── test_api.py
│   └── test_checker.py
└── docs/                    # 完整文档源码（Markdown 格式）
    ├── user_guide.md
    ├── theme_development.md
    ├── deployment.md
    └── api_reference.md
```

## 贡献指南

我们欢迎并感谢任何形式的贡献，包括但不限于功能建议、代码修复、文档改进以及资源列表的补充。请遵循以下步骤参与项目协作。

1. 在 GitHub 上 fork 本仓库至个人账号，并 clone 到本地开发环境。建议在新建的 feature 分支上进行修改，分支命名采用 <code>feature/简短描述</code> 或 <code>fix/问题编号</code> 格式。

2. 运行测试套件确保现有功能不受影响：<code>python manage.py test</code>。若新增功能或修复缺陷，请同步添加或更新对应的测试用例，覆盖核心逻辑路径。

3. 提交代码前执行代码风格检查工具（Flake8 与 Black），保持与项目现有风格一致。提交信息请遵循约定式提交规范（Conventional Commits），例如 <code>feat: 添加按点击量排序选项</code> 或 <code>fix: 修复标签过滤在空状态下的异常</code>。

4. 将分支推送至个人远程仓库，并通过 GitHub 界面发起 Pull Request。请求描述中请清晰说明变更目的、实现方式及影响范围，若有相关 issue 请一并关联。

5. 项目维护者将在 3 个工作日内进行代码审阅，并根据审阅意见进行迭代调整。合并后您的贡献将列入项目贡献者列表（如有需要，可在 PR 中注明希望使用的署名方式）。

## 常见问题

**问：系统能否同时管理数千条资源链接？页面加载性能如何？**

答：WebIndex 的静态生成模式支持数千条资源的管理规模。在生成静态页面时，系统会将所有资源数据预渲染为纯 HTML，首页加载仅涉及单次请求，无额外 API 调用。对于包含数千条记录的场景，建议开启分页配置（默认每页 50 条）并启用浏览器端缓存策略。健康检查模块采用独立异步队列执行，不会阻塞前端请求。

**问：如何从旧版书签文件或浏览器收藏夹批量迁移数据？**

答：项目提供 <code>manage.py import</code> 命令，支持 Netscape 书签格式（HTML）以及通用 CSV 文件的导入。您可以从浏览器导出书签为 HTML 文件，随后执行 <code>python manage.py import --source bookmarks.html --format netscape</code> 即可完成迁移。导入过程中系统会自动尝试解析标题、URL 及创建时间，并映射至标签体系。对于自定义字段，可通过 <code>--map</code> 参数指定列名对应关系。

**问：静态生成的站点如何更新内容？是否需要重新部署整个站点？**

答：每次资源列表发生变更（增删改操作）后，需重新执行 <code>python manage.py build</code> 命令生成最新的静态文件。部署时仅需将 <code>./dist</code> 目录下的变更文件同步至 Web 服务器即可，无需全量覆盖。项目提供了增量构建选项 <code>--incremental</code>，仅重新渲染受影响的页面，大幅缩短大型站点的构建时间。若配合 CI/CD 流水线，可在数据变更后自动触发构建与同步流程。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-24 22:42:33
